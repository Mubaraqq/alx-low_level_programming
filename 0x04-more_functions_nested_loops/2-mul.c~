#include "main.h"

/**
 * mul -  multiplies two integers
 * @a: first input
 * @b: second input
 * Return: return the result
 */

int mul(int a, int b)
{
	int result;

	result = a*b;

	return (result);
}
